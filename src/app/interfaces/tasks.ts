export interface Task {
    id: number;
    title: string;
    description: string;
    priority: string;
    clasification: string;
    estimated_hours: number;
    dedicated_hours: number;
    date_init: string;
}